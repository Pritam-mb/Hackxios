{
  "name": "ecosync-monorepo",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "cd ecosyc/ecosync-app && npm install && npm run build"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.18"
  }
}
